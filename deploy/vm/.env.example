DOMAIN=lotediretor.com

# Build-time public Mapbox token if the current ZoLa frontend still requires it.
# Do not commit an actual token.
MAPBOX_ACCESS_TOKEN=

POSTGRES_DB=lotediretor
POSTGRES_USER=lotediretor
POSTGRES_PASSWORD=CHANGE_ME_WITH_A_LONG_RANDOM_PASSWORD
